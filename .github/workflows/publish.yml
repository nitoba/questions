name: Publish to npm

on:
  workflow_dispatch:
    inputs:
      version:
        description: Exact package version expected in package.json
        required: true
        default: 0.1.0-rc.2
        type: string
      dry_run:
        description: Validate the release without publishing
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: https://registry.npmjs.org

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.4.2"

      - name: Verify requested version
        env:
          EXPECTED_VERSION: ${{ inputs.version }}
        run: |
          ACTUAL_VERSION="$(node -p "require('./package.json').version")"
          test "$ACTUAL_VERSION" = "$EXPECTED_VERSION" || {
            echo "Expected $EXPECTED_VERSION but package.json contains $ACTUAL_VERSION" >&2
            exit 1
          }

      - run: bun install --frozen-lockfile

      - name: Validate publishable tarball
        if: ${{ inputs.dry_run == true }}
        run: npm publish --dry-run --access public --tag next

      - name: Publish release candidate with npm Trusted Publishing
        if: ${{ inputs.dry_run == false }}
        run: npm publish --access public --tag next
